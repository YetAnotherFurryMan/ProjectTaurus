#pragma once

#define X_ENTRIES \
	X(init)       \
	X(module)     \
	X(project)    \
	X(language)   \
	X(build)      \
	X(test)       \
	X(bootstrap)

#define X_MODULE_ENTRIES \
	X(init)              \
	X(add)               \
	X(list)

#define X_PROJECT_ENTRIES \
	X(init)               \
	X(add)                \
	X(list)

#define X_LANGUAGE_ENTRIES \
	X(add)                 \
	X(list)

